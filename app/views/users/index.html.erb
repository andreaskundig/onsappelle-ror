<h1>Users for reminder <%= @reminder.id %></h1>

<ul>
  <% @reminder.users.each do |user| %>
      <li>
          <strong><%= user.id %></strong>
          <%= link_to user.email,
           reminder_user_path(@reminder, user) %>
      </li>
  <% end %>
</ul>
